groups:
  - name: test.rules
    rules:
      - alert: AlwaysFiring
        expr: vector(1)
        for: 0m
        labels:
          severity: page
        annotations:
          summary: "Always firing alert (for testing)"
          description: "This alert is intentionally always firing to test Alertmanager routing."
